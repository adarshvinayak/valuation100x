# Optimized Requirements for Railway with Heavy Analysis Support
# Balanced approach: Core heavy ML without the largest dependencies

# FastAPI Server
fastapi>=0.100.0
uvicorn>=0.23.0
pydantic>=2.0.0
python-multipart>=0.0.5
aiofiles>=23.0.0

# OpenAI (essential for embeddings and LLM)
openai>=1.0.0

# Core LlamaIndex
llama-index>=0.9.0
llama-index-llms-openai>=0.1.0
llama-index-embeddings-openai>=0.1.0
llama-index-vector-stores-faiss>=0.1.0

# Vector database (CPU version for Railway)
faiss-cpu>=1.7.0

# API clients
tavily-python>=0.3.0
sec-api>=1.0.0
aiohttp>=3.8.0

# Data processing essentials
pandas>=2.0.0
numpy>=1.24.0
requests>=2.28.0

# Financial data
yfinance>=0.2.0

# Core utilities
python-dotenv>=1.0.0
tenacity>=8.0.0
pyyaml>=6.0.0
redis>=4.5.0

# Database & Storage
supabase>=2.3.0

# Web scraping
beautifulsoup4>=4.12.0

# Document processing
pdfplumber>=0.9.0

# CLI utilities
click>=8.0.0
tqdm>=4.65.0
colorama>=0.4.0
rich>=13.0.0

# Heavy ML Dependencies - Optimized for Railway
# Use lighter versions to stay within build limits
sentence-transformers>=2.2.0
scikit-learn>=1.3.0

# Conditional heavy dependencies (will gracefully fallback if not available)
# transformers>=4.30.0  # Commented out - too large for Railway
# torch>=2.0.0  # Commented out - too large for Railway

# Additional analysis tools
lxml>=4.9.0