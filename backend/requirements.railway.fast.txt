# Ultra-Fast Railway Requirements - Pre-built Wheels Only
# Optimized for Railway's build time limits

# FastAPI Server (fast installs)
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
python-multipart==0.0.6
aiofiles==23.2.0

# Redis (essential for caching and sessions)
redis==5.0.1

# OpenAI (essential for embeddings)
openai==1.3.7

# Core utilities (fast installs)
python-dotenv==1.0.0
requests==2.31.0
aiohttp==3.9.1

# Data processing (use specific versions with wheels)
pandas==2.1.4
numpy==1.24.4

# Database & Storage
supabase==2.3.4

# Web scraping
beautifulsoup4==4.12.2

# Document processing
pdfplumber==0.10.3

# CLI utilities
click==8.1.7
tqdm==4.66.1
colorama==0.4.6

# API clients (lightweight versions)
tavily-python==0.3.3
sec-api==1.0.17

# Financial data
yfinance==0.2.28

# YAML processing
pyyaml==6.0.1

# Async utilities
tenacity==8.2.3

# NOTE: Heavy ML dependencies removed for fast build
# - sentence-transformers (causes timeout)
# - faiss-cpu (causes timeout) 
# - llama-index (causes timeout)
# 
# These will be handled via lazy loading or runtime installation
