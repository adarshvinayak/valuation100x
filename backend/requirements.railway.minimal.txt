# Ultra-Minimal Requirements for Railway (Emergency Fallback)
# Use this if the full requirements.railway.txt still fails

# FastAPI Server (Essential)
fastapi>=0.100.0
uvicorn>=0.23.0
pydantic>=2.0.0
python-multipart>=0.0.5

# Core utilities
python-dotenv>=1.0.0
requests>=2.28.0
aiohttp>=3.8.0

# Data processing (minimal)
pandas>=2.0.0
numpy>=1.24.0

# API clients (essential)
openai>=1.0.0

# Database
supabase>=2.3.0

# Optional: Add Redis if needed
redis>=4.5.0

# Note: This minimal setup will disable:
# - Local embeddings (sentence-transformers)
# - Vector search (faiss-cpu)
# - Advanced LlamaIndex features
# But the API will still work for basic operations
