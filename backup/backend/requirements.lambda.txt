# Optimized Requirements for AWS Lambda Deployment
# Lightweight dependencies to minimize cold start time

# FastAPI & ASGI adapter for Lambda
fastapi>=0.100.0
uvicorn>=0.23.0
mangum>=0.17.0  # ASGI to Lambda adapter
pydantic>=2.0.0
python-multipart>=0.0.5
aiofiles>=23.0.0

# OpenAI (essential for embeddings and LLM)
openai>=1.0.0

# Core LlamaIndex (lightweight)
llama-index>=0.9.0
llama-index-llms-openai>=0.1.0
llama-index-embeddings-openai>=0.1.0

# API clients
aiohttp>=3.8.0
httpx>=0.25.0
requests>=2.28.0

# Data processing essentials
pandas>=2.0.0
numpy>=1.24.0

# Financial data
yfinance>=0.2.0

# Core utilities
python-dotenv>=1.0.0
tenacity>=8.0.0
pyyaml>=6.0.0

# Database
supabase>=2.3.0

# Web scraping (lightweight)
beautifulsoup4>=4.12.0

# Document processing
pdfplumber>=0.9.0

# API clients for financial data
tavily-python>=0.3.0
sec-api>=1.0.0

# Note: Heavy dependencies removed for Lambda optimization:
# - faiss-cpu (use managed vector DB instead)
# - redis (use AWS ElastiCache or DynamoDB)
# - torch/transformers (use OpenAI APIs instead)
# - sentence-transformers (use OpenAI embeddings)


